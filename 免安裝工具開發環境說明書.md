# Windows 免安裝工具開發環境說明書

> 參考專案：Keyviz 鍵盤按鍵顯示器與螢幕繪圖
>
> 文件日期：2026-07-31
>
> 適用目標：Windows 10／11 x64、單一免安裝 EXE、系統匣工具、多螢幕透明覆蓋層

## 1. 文件目的

本文件整理 Keyviz 目前實際使用的開發環境、程式架構、建置工具、GitHub Actions 與免安裝 EXE 發布流程，讓其他 Windows 桌面工具專案可以直接參考。

Keyviz 採用的主要方案是：

```text
React + TypeScript + Vite
            │
            ▼
       Tauri 2 WebView
            │
            ▼
 Rust 原生 Windows 功能與透明覆蓋視窗
            │
            ├─ Direct2D 軟體渲染 + WIC + DirectWrite：按鍵顯示
            ├─ Layered Window + DIB/GDI：螢幕繪圖與部分透明覆蓋層
            └─ rdev：全域鍵盤與滑鼠事件
```

此方案適合需要以下特性的工具：

- 檔案小於一般 Electron 應用程式。
- 不需要安裝精靈，下載 EXE 後即可執行。
- 需要系統匣、全域快速鍵、多螢幕及永遠置頂視窗。
- 需要透明、滑鼠穿透或錄影相容的 Windows 原生覆蓋層。
- 設定介面希望使用 React、CSS 與成熟的前端元件。

## 2. 免安裝版的定義與限制

本專案的「免安裝版」是由 Tauri `--no-bundle` 產生的單一 Windows EXE：

```text
release\keyviz-portable.exe
```

使用者不需要執行 MSI、NSIS 或其他安裝程式，也不會把程式檔案安裝到 `Program Files`。

但必須理解以下限制：

- 設定仍會寫入目前 Windows 使用者的應用程式資料目錄。
- Tauri 設定介面需要 Microsoft Edge WebView2 Runtime。
- Windows 10／11 通常已內建 WebView2；精簡版 Windows 需另行確認。
- 未簽章 EXE 可能顯示 Microsoft Defender SmartScreen 警告。
- 「單一 EXE」不等於完全不留下任何使用者設定或快取。

## 3. 建議與目前實際環境

### 3.1 建議的可重現建置環境

| 項目 | 建議版本或設定 |
|---|---|
| 作業系統 | Windows 11 x64，Windows 10 x64 亦可 |
| PowerShell | Windows PowerShell 5.1 或 PowerShell 7 |
| Node.js | Node.js 22 LTS，與 GitHub Actions 一致 |
| npm | 隨 Node.js 安裝，使用 `npm.cmd` 執行 |
| Rust | Stable，目標為 `x86_64-pc-windows-msvc` |
| C++ 工具鏈 | Visual Studio Build Tools 2022 |
| Windows SDK | Windows 10 SDK 或 Windows 11 SDK |
| Git | Git for Windows |
| GitHub CLI | `gh`，僅發布與下載雲端產物時需要 |
| 執行階段 | Microsoft Edge WebView2 Runtime |

### 3.2 本機盤點時的版本

```text
Node.js       v25.9.0
npm           11.17.0
rustc         1.96.0
cargo         1.96.0
Rust toolchain stable-x86_64-pc-windows-msvc
Git           2.48.1.windows.1
GitHub CLI    2.92.0
```

本機 Node.js 版本可以進行前端建置，但正式可重現流程以 GitHub Actions 的 Node.js 22 為準。

### 3.3 Visual Studio Build Tools 必要元件

安裝 Visual Studio Build Tools 2022 時，至少勾選：

- Desktop development with C++／使用 C++ 的桌面開發。
- MSVC v143 x64/x86 build tools。
- Windows 10 SDK 或 Windows 11 SDK。
- C++ CMake tools 可選，Keyviz 本身不強制需要。

若缺少 MSVC linker，Rust/Tauri 會出現：

```text
error: linker `link.exe` not found
```

這不是 Rust 程式碼錯誤；必須安裝 C++ Build Tools，或改用 GitHub Actions 的 `windows-latest` 建置。

## 4. 核心技術與鎖定版本

### 4.1 前端設定介面

| 技術 | package.json 範圍 | package-lock 鎖定版本 | 用途 |
|---|---:|---:|---|
| React | `^19.1.0` | `19.2.3` | 設定介面與元件狀態 |
| React DOM | `^19.1.0` | `19.2.3` | React 視窗渲染 |
| TypeScript | `~5.8.3` | `5.8.3` | 型別與編譯檢查 |
| Vite | `^7.0.4` | `7.3.0` | 開發伺服器與前端打包 |
| Tauri API | `^2` | `2.9.1` | 前端呼叫原生 Rust command |
| Tauri CLI | `^2` | `2.9.6` | 開發與建置 EXE |
| Tailwind CSS | `^4.1.18` | `4.1.18` | 設定介面樣式 |
| Zustand | `^5.0.9` | `5.0.9` | 設定狀態與持久化 |
| Lucide React | `^0.562.0` | `0.562.0` | 工具列圖示 |

設定頁也使用 Radix UI、shadcn 類型元件、Hugeicons 與 Tauri plugins。

### 4.2 Rust 與 Tauri 原生端

| 技術 | 鎖定版本 | 用途 |
|---|---:|---|
| Tauri | `2.9.5` | 視窗、WebView、系統匣、命令橋接 |
| tauri-build | `2.5.3` | Rust 建置整合 |
| windows crate | `0.48.0` | Win32、Direct2D、DirectWrite、WIC、DWM、GDI |
| serde | `1.0.228` | Rust 設定資料序列化 |
| serde_json | `1.0.145` | JSON 設定解析 |
| tauri-plugin-store | `2.4.1` | 使用者設定保存 |
| tauri-plugin-single-instance | `2.3.7` | 避免重複啟動 |
| tauri-plugin-prevent-default | `4.0.3` | WebView 預設行為控制 |
| rdev | 專案內本地 crate | 全域鍵盤與滑鼠事件 |

Rust release profile 使用：

```toml
[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
```

這些設定可減少 EXE 體積，但會增加 release 編譯時間。

## 5. 為什麼按鍵顯示改用 Windows 原生軟體渲染

透明 WebView 或 GPU 合成在螢幕錄影、硬體加速切換、多顯示卡與混合 DPI 環境中，可能出現：

- 透明區域變成白色或黑色方塊。
- 視窗邊界被錄影程式擷取。
- GPU 紋理殘留。
- 錄影開始後按鍵覆蓋層消失或程式異常結束。

目前按鍵顯示層位於：

```text
src-tauri\src\app\native_keys_software.rs
```

採用：

- Direct2D `D2D1_RENDER_TARGET_TYPE_SOFTWARE`，強制使用軟體渲染。
- WIC 建立 `32bppPBGRA` 點陣圖。
- DirectWrite 繪製文字。
- `UpdateLayeredWindow` + `AC_SRC_ALPHA` 輸出透明視窗。
- 預乘 Alpha（premultiplied alpha）像素格式。

這個設計讓按鍵覆蓋層不依賴透明 WebView 的 GPU 合成，對瀏覽器錄影、DWM 與硬體加速變化較穩定。

### 重要：不同覆蓋層不是同一套後端

- 按鍵顯示：Direct2D 軟體 + WIC + DirectWrite。
- 螢幕繪圖：原生透明 Layered Window、DIB/GDI 與物件模型，位於 `native_drawing.rs`。
- 游標圓環：獨立原生透明視窗，位於 `native_cursor.rs`。
- 設定介面：Tauri WebView2 + React。

其他專案移植時，應先判斷哪一個視窗需要錄影相容性，不必把所有設定介面都改成原生 Win32。

## 6. 專案結構與責任分工

```text
keyviz-current-work
├─ package.json                     前端指令與 npm 相依套件
├─ package-lock.json                前端可重現版本
├─ vite.config.ts                   Vite 與 Tauri 開發設定
├─ README.md                        使用者功能說明
├─ PORTABLE.md                      簡要免安裝建置說明
├─ release
│  └─ keyviz-portable.exe           對外提供的免安裝執行檔
├─ src
│  ├─ components\settings           各設定分頁
│  ├─ lib\i18n.ts                   繁體中文／英文翻譯
│  ├─ pages\screen-drawing.tsx      繪圖工具列 WebView 介面
│  └─ stores                         Zustand 與 Tauri Store 設定
├─ src-tauri
│  ├─ Cargo.toml                    Rust 相依套件與 release profile
│  ├─ Cargo.lock                    Rust 可重現版本
│  ├─ tauri.conf.json               Tauri 視窗、版本與圖示設定
│  ├─ crates\rdev                   專案內調整過的全域輸入監聽
│  └─ src\app
│     ├─ commands.rs                前端可呼叫的 Tauri commands
│     ├─ event.rs                   全域輸入事件與快捷鍵處理
│     ├─ state.rs                   應用程式共享狀態與設定載入
│     ├─ window.rs                  多螢幕與視窗定位
│     ├─ native_keys.rs             按鍵覆蓋層入口
│     ├─ native_keys_software.rs    D2D/WIC/DirectWrite 軟體渲染
│     ├─ native_cursor.rs           游標圓環覆蓋層
│     └─ native_drawing.rs          原生螢幕繪圖與可編輯物件
└─ .github\workflows
   ├─ build-portable.yml            Windows 雲端建置
   └─ winget.yml                    Release 發布後的 Winget 流程
```

## 7. 建立本機開發環境

### 7.1 安裝必要工具

1. 安裝 Git for Windows。
2. 安裝 Node.js 22 LTS。
3. 安裝 Rustup 與 stable MSVC toolchain。
4. 安裝 Visual Studio Build Tools 2022 的 C++ 桌面開發工作負載。
5. 確認 Windows SDK 已安裝。
6. 如需上傳 GitHub，再安裝 GitHub CLI。

設定 Rust toolchain：

```powershell
rustup toolchain install stable-x86_64-pc-windows-msvc
rustup default stable-x86_64-pc-windows-msvc
```

檢查工具：

```powershell
node --version
npm.cmd --version
rustc --version
cargo --version
git --version
where.exe link.exe
```

### 7.2 取得專案與安裝相依套件

```powershell
git clone https://github.com/harmonica80/keyviz-multi-monitor.git
Set-Location keyviz-multi-monitor
npm.cmd ci
```

正式建置建議使用 `npm.cmd ci`，依照 `package-lock.json` 安裝；只有調整套件版本時才使用 `npm.cmd install`。

在 Windows PowerShell 中使用 `npm.cmd`，可避免執行原則封鎖 `npm.ps1`。

## 8. 開發與檢查指令

### 8.1 啟動開發模式

```powershell
npm.cmd run tauri dev
```

Tauri 會啟動 Vite 開發伺服器，預設使用：

```text
http://localhost:1420
```

### 8.2 只檢查前端

```powershell
npm.cmd run build
```

此命令會執行：

```text
tsc && vite build
```

可檢查 TypeScript 型別、React 程式與正式前端打包。

### 8.3 檢查 Rust

```powershell
cargo fmt --manifest-path src-tauri\Cargo.toml -- --check
cargo check --manifest-path src-tauri\Cargo.toml
```

另外執行 Git 差異檢查：

```powershell
git diff --check
```

## 9. 建置單一免安裝 EXE

專案已定義：

```json
"build:portable": "tauri build --no-bundle"
```

執行：

```powershell
npm.cmd run build:portable
```

Tauri 會先執行前端 production build，再編譯 Rust release。原始結果位於：

```text
src-tauri\target\release\keyviz.exe
```

複製成對外檔名：

```powershell
New-Item -ItemType Directory -Force release
Copy-Item src-tauri\target\release\keyviz.exe release\keyviz-portable.exe -Force
```

`--no-bundle` 是關鍵：它只產生可執行檔，不建立 MSI 或 NSIS 安裝包。

## 10. GitHub Actions 雲端建置

當開發電腦沒有 `link.exe`，或希望建立一致的正式產物時，使用：

```text
.github\workflows\build-portable.yml
```

目前流程：

1. 使用 `windows-latest`。
2. Checkout 原始碼。
3. 安裝 Node.js 22。
4. 安裝 stable Rust MSVC target。
5. 執行 `npm ci`。
6. 執行 `npm run build:portable`。
7. 將 `keyviz.exe` 複製成 `release/keyviz-portable.exe`。
8. 上傳名為 `keyviz-portable` 的 Actions artifact。

推送到 `main` 會自動觸發，也可以在 GitHub Actions 手動執行 `workflow_dispatch`。

使用 GitHub CLI 查詢及下載：

```powershell
gh run list --workflow build-portable.yml --branch main --limit 5
gh run watch RUN_ID --exit-status
gh run download RUN_ID -n keyviz-portable -D artifact-RUN_ID
```

## 11. 更新 GitHub Release 下載檔

確認 EXE 後，以相同檔名更新既有 Release：

```powershell
gh release upload v1.0.0 release\keyviz-portable.exe --clobber
gh release view v1.0.0 --json assets,url
```

建立 SHA-256：

```powershell
Get-FileHash release\keyviz-portable.exe -Algorithm SHA256
```

發布前應確認 GitHub Release 的 `digest` 與本機 SHA-256 完全一致。

## 12. 建置後基本檔案驗證

```powershell
$file = Get-Item release\keyviz-portable.exe
$hash = Get-FileHash $file.FullName -Algorithm SHA256
$bytes = [System.IO.File]::ReadAllBytes($file.FullName)

[pscustomobject]@{
    Path = $file.FullName
    SizeMB = [math]::Round($file.Length / 1MB, 2)
    PEMagic = ('{0:X2}{1:X2}' -f $bytes[0], $bytes[1])
    ProductVersion = $file.VersionInfo.ProductVersion
    FileVersion = $file.VersionInfo.FileVersion
    SHA256 = $hash.Hash
}
```

正常 Windows EXE 的 `PEMagic` 應為：

```text
4D5A
```

## 13. 必做的執行測試矩陣

只通過 CI 不代表透明覆蓋層在所有 Windows 環境都正確。至少測試：

- 單螢幕與多螢幕。
- 第二螢幕位於主螢幕左、右、上、下方。
- 螢幕原點包含負座標。
- 100%、125%、150% 混合 DPI 縮放。
- 鍵盤一般按鍵、修飾鍵、組合鍵與滑鼠按鍵。
- 按鍵顯示位置及工具列左／右側開啟設定。
- F8 開啟／關閉繪圖、F7 回到游標模式。
- 畫筆、文字、序號、選取、群組、旋轉、清除與復原。
- 瀏覽器線上錄影、OBS 或 Windows 錄影期間的按鍵覆蓋層。
- 開始錄影前後切換硬體加速。
- 關閉設定視窗後，系統匣程式仍正常運作。
- 將 EXE 複製到另一個資料夾後直接執行。

透明視窗問題必須在實際 Windows/DWM/錄影路徑測試；`cargo fmt`、`cargo check` 或 CI 成功不能取代執行測試。

## 14. 多螢幕與透明覆蓋層設計原則

### 多螢幕

- 使用整個 virtual desktop 的最小與最大實體座標。
- 不要假設主螢幕座標一定是 `(0, 0)`。
- 左側或上方螢幕可能使用負座標。
- 視窗尺寸與位置應使用一致的實體像素或邏輯像素單位。
- 計算工具列右側位置時，要納入該螢幕的 DPI scale factor。

### Layered Window Alpha

- `UpdateLayeredWindow` 搭配 `AC_SRC_ALPHA` 時使用預乘 Alpha。
- 完全透明像素的 RGB 應避免殘留可見色值。
- 每次更新應完整清除或明確重畫透明畫布。
- DIB stride、來源尺寸、目標尺寸必須一致。
- 錄影程式可能改變 DWM、硬體游標及透明視窗的擷取路徑，必須另外驗證。

### 滑鼠穿透

- 工具列視窗不可跟著繪圖畫布一起穿透。
- 繪圖模式與游標模式切換時，保持視窗生命週期穩定。
- 避免每次滑鼠移動都建立或銷毀透明視窗。
- 自訂游標切換時，先把新游標交給 Windows，再安全釋放舊資源。

## 15. 設定保存與語系

Keyviz 使用：

```text
Zustand + @tauri-apps/plugin-store
```

設定寫入 `store.json`，並由設定視窗同步給其他 Tauri 視窗。設計其他專案時建議：

- 每個 store 都加入 schema version。
- 新增欄位時提供 migration 與預設值。
- 不要因缺少新欄位而讓舊設定無法載入。
- 只有一個視窗負責寫入，其他視窗透過事件同步。
- 顯示文字集中在單一 i18n 檔案，不在元件中散落硬編碼翻譯。

## 16. 版本管理注意事項

目前專案可看到三個版本來源：

```text
package.json              0.1.0
src-tauri\Cargo.toml      0.1.0
src-tauri\tauri.conf.json 1.0.0
```

其他專案正式發布前，建議統一三處版本，避免：

- 設定頁顯示版本與 EXE 檔案版本不同。
- GitHub Release tag 與程式版本不同。
- 自動檢查更新判斷錯誤。

可將版本更新納入發布腳本或 CI 檢查。

## 17. 檔案大小控制

Keyviz 的免安裝 EXE 約 6 MB，主要原因：

- Tauri 使用系統 WebView2，不內嵌完整 Chromium。
- Rust release 啟用 LTO、strip 與單一 codegen unit。
- 不使用大型影音模型或內嵌瀏覽器核心。
- 只建立 `--no-bundle` EXE，不附加安裝器。

減少體積時不要任意刪除：

- `package-lock.json`、`Cargo.lock`。
- Tauri icons 與 resource。
- 專案內修改過的 `crates/rdev`。
- Windows 原生繪圖模組。

可安全不納入 Git 的建置產物：

```text
node_modules
dist
src-tauri\target
.npm-cache
artifact-*
```

## 18. 安全與發布建議

- 不要把 GitHub token、私鑰或憑證提交到 Git。
- GitHub CLI 登入資料保留在使用者電腦，不寫入專案文件。
- Winget token 應使用 GitHub Actions Secrets。
- 正式大規模發布建議使用程式碼簽章憑證。
- Release 應提供 SHA-256，讓使用者可驗證下載檔。
- 發布前使用 Windows Defender 掃描 EXE。

## 19. 其他專案選型建議

### 適合沿用 Tauri + Rust 的情況

- 需要較小的免安裝 EXE。
- UI 可以使用 Web 技術，但核心功能需要 Win32。
- 需要系統匣、全域快捷鍵、透明視窗或多螢幕。
- 希望把錄影敏感的覆蓋層改成原生軟體渲染。

### 可能更適合 Electron 的情況

- 高度依賴 Chromium 媒體 API、WebRTC 或瀏覽器擴充功能。
- 可以接受數十到數百 MB 的檔案。
- 團隊只使用 JavaScript，不希望維護 Rust/Win32。

### 可能更適合純 WinUI／WPF／Win32 的情況

- 所有介面都必須是 Windows 原生控制項。
- 不希望依賴 WebView2。
- 團隊熟悉 C#、C++ 或 Windows App SDK。

## 20. 新專案快速檢查表

- [ ] 決定 Tauri、Electron 或純原生 Windows 架構。
- [ ] 建立 `package-lock.json` 與 `Cargo.lock`。
- [ ] 使用 Node.js LTS 與 Rust stable MSVC。
- [ ] 安裝 Visual C++ Build Tools 與 Windows SDK。
- [ ] 先完成 `npm.cmd run build` 與 `cargo check`。
- [ ] 用 `tauri build --no-bundle` 產生單一 EXE。
- [ ] 建立 GitHub Actions `windows-latest` 建置。
- [ ] 驗證 EXE 的 MZ/PE、大小、版本與 SHA-256。
- [ ] 測試多螢幕負座標與混合 DPI。
- [ ] 實測螢幕錄影中的透明覆蓋層。
- [ ] 確認設定可遷移且舊版本不會損壞。
- [ ] 統一 package、Cargo、Tauri 與 Release 版本。
- [ ] 更新 README、Release 說明與直接下載連結。

## 21. Keyviz 目前使用的正式建置摘要

```powershell
# 安裝鎖定相依套件
npm.cmd ci

# 前端與型別檢查
npm.cmd run build

# Rust 格式與程式檢查
cargo fmt --manifest-path src-tauri\Cargo.toml -- --check
cargo check --manifest-path src-tauri\Cargo.toml
git diff --check

# 建立單一免安裝 EXE
npm.cmd run build:portable

# 整理交付檔
New-Item -ItemType Directory -Force release
Copy-Item src-tauri\target\release\keyviz.exe release\keyviz-portable.exe -Force

# 驗證檔案
Get-FileHash release\keyviz-portable.exe -Algorithm SHA256
```

若本機沒有 MSVC `link.exe`，保留前端與格式檢查，將完整 Windows release build 交由 GitHub Actions 執行。
